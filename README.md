# Трекер полезных привычек
Этот проект представляет собой backend-часть SPA веб-приложения для отслеживания полезных привычек.
Функционал приложения позволяет добавлять пользователям свои привычки, указывать к ним награду
в виде чего-то приятного или выполнения приятной привычки. Так же пользователи могут делать свои
привычки публичными. Пользователь может настроить напоминание о выполнении действий через телеграм бота.
При подключении телеграм бота к профилю пользователя происходит подтверждение Telegram ID
с помощью кода верификации.

## Порядок установки и запуска проекта локально:

Для запуска проекта необходимо клонировать репозиторий на ваш компьютер, установить все необходимые
программы, установить все зависимости, создать бота в Telegram, заполнить файл .env
и выполнить миграции.
Для тестов можно добавить тестовые данные.

### Необходимые программы:
    
    - PostgreSQL

    - Python 3.x

    - Redis

    - Docker

### Install project dependencies:
```commandline
poetry install
```

### Выполнение миграций

Перед выполнением миграций вам необходимо вручную создать базу данных в PostgreSQL и занести данные в
файл .env согласно шаблону(.env_example).
Для выполнения миграций введите в терминале команду:
    
    python manage.py migrate

### Верификация Telegram пользователя

Для активации напоминаний через Telegram пользователь должен верифицировать свой аккаунт вводом кода.
Код автоматически присылается пользователю при регистрации в приложении или при добавлении Telegram username
при редактировании профиля, но при условии, что пользователь до этого начал чат с ботом командой /start.

Ввод кода верификации происходит после авторизации пользователя на странице
    
    %user_id%/telegram_verification

### Документация

Документацию к проекту можно посмотреть по эндпоинтам:
    
    /swagger/
    /redoc/

### Запуск сервера

Для запуска сервера на удаленной виртуальной машине необходимо:

#### 1 Создать виртуальную машину (например на Yandex Cloud)
#### 2 Установить на ВМ Git и Docker
#### 3 Клонировать репозиторий командой git clone <ссылка_на_репозиторий>
#### 4 Создать в папке проекта файл .env и заполнить его своими данными
#### 5 Распаковать Docker контейнеры командой docker-compose up
Если после выполнения команды docker-compose up сервер не работает, попробуйте остановить и удалить контейнеры,
затем пересоздать их и распаковать заново
    
    docker-compose down -v - останавливает и удаляет контейнеры

    docker-compose build - пересобирает контейнеры заново

    docker-compose up -d - распаковывает контейнеры и запускает сервер

После запуска сервера приложение будет доступно по адресу: http://публичный_ip_вашей_вм:80/

Если команды git clone и docker-compose up не выполняются, перед каждой командой пропишите sudo

Для остановки сервера нажмите клавиши Ctrl+C или выполните команду docker-compose stop

Пример работы приложения расположен на сервере http://51.250.17.3:80/

Для работы CI/CD необходимо в GitHub secrets перенести все переменные из .env и указать переменные

    DEPLOY_DIR - директория на ВМ в которой будет находиться проект
    DOCKER_HUB_ACCESS_TOKEN - access token с docker hub
    DOCKER_HUB_USERNAME - ваше имя пользователя docker hub
    SSH_KEY - приватный ssh ключ для подключения к ВМ
    SSH_USER - ваше имя пользователя для подключения к ВМ
    SERVER_IP - публичный IP адрес вашей ВМ